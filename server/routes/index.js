// ─── ROUTE REGISTRY ─────────────────────────────────────────────────────────
// NOTE: All routes are defined inline in server/index.js (monolithic architecture).
// This file exists only for import compatibility — it is a no-op.
// The actual route registration happens directly in server/index.js.
export default function registerRoutes(app) {
  // All routes are registered inline in server/index.js
  console.log('[Routes] Using monolithic route registration from server/index.js');
}